# Push to GitHub

```bash
cd Red-Range5
git init
git add .
git commit -m "APT36 Operation PHANTOM RELAY — NTLM Relay & Delegation Cyber Range (GRIDFALL)"
git branch -M main
git remote add origin git@github.com:hacktifytechnologies/Red-Range5.git
git push -u origin main
```
